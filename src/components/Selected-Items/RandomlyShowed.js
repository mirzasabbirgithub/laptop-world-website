import React from 'react';

const RandomlyShowed = ({ random }) => {
          return (
                    <div>
                              <h2>{random.name}</h2>
                    </div>
          );
};

export default RandomlyShowed;